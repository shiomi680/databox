// import { Item, Image, FileModel PrismaClient, Prisma } from '@prisma/client'

// export const getImagePath = async (imageId: number): Promise<string | null> => {
//   const res = await fetch(`/api/image/${imageId}`)
//   const data: Image & {
//     FileTable: FileModel
//   } = await res.json()
//   return data.FileTable.FilePath
// }

// export const postImage = async (imageId: number): Promise<string | null> => {
//   const res = await fetch(`/api/image/${imageId}`)
//   const data: Image & {
//     FileTable: FileModel
//   } = await res.json()
//   return data.FileTable.FilePath
// }
